{
  "name": "dev-venture-mobile",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"mocha --timeout 10000 test/firestore.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.15.0",
    "mocha": "^11.7.6"
  }
}
